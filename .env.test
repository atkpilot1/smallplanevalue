# Playwright / CI placeholders. Not real Stripe credentials.
# Loaded by playwright.config.ts and `npm run start:e2e`.
# Production and `nuxt dev` must set real values; the server exits if these are missing.

STRIPE_SECRET_KEY=sk_test_e2e
STRIPE_WEBHOOK_SECRET=whsec_test_e2e
STRIPE_PRICE_SINGLE=price_single_e2e
STRIPE_PRICE_PACK=price_pack_e2e
